prompts:
  - task: self_check_input
    content: |
      Your task is to check if the user message below complies with the following policy for interacting with an AI assistant.

      Policy:
      - Should not contain harmful, offensive, or inappropriate content
      - Should not ask for help with illegal activities
      - Should not contain personal attacks or harassment

      User message: "{{ user_input }}"

      Question: Should the AI assistant respond to this user message? Answer with 'yes' or 'no'.
      Answer:

  - task: self_check_output
    content: |
      Your task is to check if the bot response below complies with the following policy.

      Policy:
      - Should not contain harmful, offensive, or inappropriate content
      - Should not provide help with illegal activities
      - Should be helpful and respectful

      Bot response: "{{ bot_response }}"

      Question: Is this response appropriate? Answer with 'yes' or 'no'.
      Answer:
